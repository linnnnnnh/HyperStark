
image: node

before_script:
  - npm install

cache:
  paths:
    - node_modules/

pages:
  stage: deploy
  script:
    - unset CI
    - BASE_PATH=6676f75ff71a2f000877c4ed/ npm run build
    - mv public public2
    - mv build public
  artifacts:
    paths:
      - public
  only:
    - master
  